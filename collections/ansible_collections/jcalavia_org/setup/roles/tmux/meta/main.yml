galaxy_info:
  author: 'Jorge Calavia'
  description: 'Role to install tmux'
  company: 'Calavia Org'
  license: 'GPL-3.0-only'
  min_ansible_version: '2.19'

  # https://galaxy.ansible.com/api/v1/platforms/
  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags:
    - dev
    - tmux
    - sysops
    - terminal
    - productivity

dependencies:
  - role: git
    git_skip_configure: true
