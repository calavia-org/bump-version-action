---
- name: 'Update apt cache...'
  when: ansible_pkg_mgr == 'apt'
  become: '{{ nvim_privilege_escalation }}'
  become_user: root
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: 'Install neovim os pkgs...'
  become: '{{ nvim_privilege_escalation }}'
  become_user: root
  ansible.builtin.package:
    name: '{{ nvim_os_pkgs }}'
    state: present
