---
- name: 'Update apt cache...'
  when: ansible_pkg_mgr == 'apt'
  become: '{{ opencode_privilege_escalation }}'
  become_user: root
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: 'Install OpenCode os pkgs...'
  become: '{{ opencode_privilege_escalation }}'
  become_user: root
  ansible.builtin.package:
    name: '{{ opencode_os_pkgs }}'
    state: present
